server:
  port: 2020

  #eureka ����
eureka:
  instance:
    hostname: 10.147.19.98 #eureka
  client:
    service-url:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/  #
    register-with-eureka: false #
    fetch-registry: false  # false